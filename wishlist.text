* Generate documentation from XML Docs that would show up in the stand-alone client
* Hold on to models that come and go so that they can be re-used in the API
* be able to store and re-call sets of headers that can be re-used in different APIs
* make the UI nicer
* precompile the handlebars templates and minify/combine all the scripts
